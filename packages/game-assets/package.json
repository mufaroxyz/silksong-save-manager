{
    "name": "@save-manager/game-data",
    "version": "0.1.0",
    "private": true,
    "files": [
        "data"
    ],
    "main": "data/index.ts",
    "scripts": {
        "build:wasm": "wasm-pack build --target web --out-dir pkg --release --features wasm",
        "build": "bun run build.ts",
        "build:direct": "wasm-pack build --target web --out-dir pkg --release",
        "test": "bun test test/"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}